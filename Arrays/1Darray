//to calculate the size of square matrix(n*n) where the elements are present at (i+j)>=n+1
import java.util.Scanner;
public class Main
{
	public static void main(String[] args) {
	    Scanner sc=new Scanner(System.in);
	    //taking the input of the matrix
	    int n=sc.nextInt();
	    //all the anti-diagonal elements and corresponding sudiagonals lower the anti-diagonal elements satisfy the condition.
        //N+N-1+N-2........1=N(N+1)/2 elements satisfy this condition
        int r=(n*(n+1))/2;
	    System.out.println(r);
	    sc.close();
		
	}
}
